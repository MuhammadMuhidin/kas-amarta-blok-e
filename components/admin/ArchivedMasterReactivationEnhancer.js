// Deprecated — all lifecycle buttons moved to MasterManagementTab.js to avoid
// React DOM reconciliation errors (removeChild / hydration #425).
export default function ArchivedMasterReactivationEnhancer() {
  return null;
}
